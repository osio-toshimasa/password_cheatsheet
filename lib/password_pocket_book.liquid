<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>PasswordPocketBook</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <script type="module">
        import {Application, Controller} from "https://unpkg.com/@hotwired/stimulus@3.2.1/dist/stimulus.js"

        window.Stimulus = Application.start()

        Stimulus.register("password", class extends Controller {
            static targets = ["result"]
            static classes = ["supported"]

            connect() {
                if ("clipboard" in navigator) {
                    this.element.classList.add(this.supportedClass);
                }
            }

            copy(event) {
                event.preventDefault()
                navigator.clipboard.writeText(this.resultTarget.value)
            }
        })
    </script>

</head>
<body>

<div class="container-fluid d-flex align-items-center justify-content-center">
    <table>
        <thead>
        <tr>
            <th>DESC</th>
            <th>URL</th>
            <th>USER</th>
            <th>PASS</th>
        </tr>
        </thead>
        <tbody>
        {% for item in password_list %}
            <tr>
                <td>
                    {{ item.desc }}
                </td>
                <td>
                    {{ item.url }}
                </td>
                <td>
                    {{ item.user }}
                </td>
                <td data-controller="password" data-password-supported-class="clipboard--supported">
                    <input data-password-target="result" class="form-control" type="text" value="{{ item.pass }}" readonly>
                    <button data-action="password#copy" class="btn btn-outline-secondary clipboard-button">クリップボードにコピー</button>
                </td>
            </tr>
        {% else %}
            <p>PASSWORD LIST EMPTY!</p>
        {% endfor %}
        </tbody>

    </table>

</div>

</body>


</html>